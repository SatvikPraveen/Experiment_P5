# Output Directory

This directory contains all training outputs:
- Checkpoints (model weights)
- Logs (training metrics, config)
- Plots (visualizations)

Each training run creates a timestamped subdirectory.
